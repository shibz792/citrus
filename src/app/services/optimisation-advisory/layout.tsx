import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Optimisation & Advisory | Citrus Global',
  description: 'Ongoing improvement, support and advisory to keep your systems performing after go-live. Measured utilisation gains and continuous improvement.',
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

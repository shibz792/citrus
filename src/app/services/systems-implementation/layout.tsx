import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Systems Implementation | Citrus Global',
  description: 'Configuration, migration, training, go-live and stabilisation. We finish the job from assessment through to a working system.',
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

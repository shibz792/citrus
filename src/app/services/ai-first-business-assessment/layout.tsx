import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'AI-First Business Assessment | Citrus Global',
  description: 'AI scans your operation to find revenue gaps, manual bottlenecks and underutilised systems. A consultant validates every finding.',
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

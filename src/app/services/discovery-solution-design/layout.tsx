import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Discovery & Solution Design | Citrus Global',
  description: 'Design the target process, choose the platform on evidence, and lock scope before anyone builds. Clarity before commitment.',
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
